# 9 月 11 日界面修复与运行核查

基线：5ad96a7，分支 codex/mac-170-ui-refresh。

后续更新：本页为初次日志核查记录。真实复现已修正华为原因并完成三条定向修复，详见 [三条渠道失败修复与真实复测](2026-09-11-channel-failures-fixed.md)。

## 本次运行核查

只读查询本地 tasks.sqlite3，未修改任务、结果或输出工作簿。
运行 a52226f3-479d-478f-b05e-bdc348b0d759：2026-09-11 09:15 开始，18 条渠道任务，15 成功、3 技术失败。三条均尝试了 3 次。

| 商品与渠道 | 本次失败位置及证据 | 结论边界 |
| --- | --- | --- |
| 华为畅享 90 Pro Max / 官网 | LAYOUT_CHANGED；诊断截图可见目标商品位于搜索结果第一张卡片，诊断 JSON 的 result_region_count=0、configured_cards=[] | 页面已有目标商品，但适配器没有成功定位搜索结果结构；不是商品不存在。现有诊断未记录底层异常文本，不能据此确定唯一失效选择器或是否存在加载时序问题。 |
| iPhone 17 / 天猫 | 搜索页：Tmall product card title structural element is missing or ambiguous | 商品卡标题定位未满足唯一可见条件。此次未保存失败 DOM/截图，不能进一步断言是广告卡、隐藏标题还是页面结构变体。 |
| 荣耀 Magic8 / 京东 | 首次搜索页、后两次店铺页：JD exact product result is ambiguous | 匹配结果得到多个不同商品链接，现有唯一链接检查拒绝继续。未保存候选明细，不能确认是不同规格还是不同商品条目。 |

9 月 4 日运行 a4aab87b-bae6-4a79-88c9-943f4c710911 的 18 条全部成功。上述三条当时均有 price_found、截图及商品 URL，历史价格分别 2199、5999、5499（仅为历史证据，不作为当前价格）。华为 9 月 9 日也曾出现同类识别失败。

站点适配代码与 .170 稳定检查点 94aaecb 无差异。由此只能确认本次 UI 改动没有直接改写取价适配器；不能单凭代码一致排除运行环境或时序影响。

本次交付修复界面问题，不更改网站筛选逻辑，不将失败记录改写为成功，也没有用历史价格代替新取价。三条真实渠道尚未复跑验收。后续适配应采集最新搜索卡片结构与候选链接，并在校验型号、规格、店铺来源后处理正常多条目，保留不确定时停止的检查。

华为诊断证据：Application Support/福建移动铺货报价助手/evidence/diagnostics/task-2ae9ba27f5288df2ccdb729dddc7d52ddfdb2a26ae112766c9054611796c1c6b.g0.a3.png 及同名 .json。

## 界面修复

- 金额原先从空值“—”布局后，通过 Configure 回调把自身狭窄宽度继续写回 wraplength，导致 ¥5,499 逐字换行。金额改为单行自然宽度；窄窗口三渠道卡片使用可容纳完整金额的字号，最低有效价也取消金额换行。
- 下拉框原先固定按每行 26px 估算高度，与 Tk 实际行高不一致。改为按实际选项数量、字体、选择边距测量高度；7 个选项不再出现多余空行。
- 空白处释放鼠标不再因 Listbox.nearest 自动误选最后一个品牌。

## 验证

- 新回归先复现缺陷：金额宽 28、高 151；7 项下拉实际内容 139px，列表占用 182px。
- 单元测试 1944 通过（40.97 秒）；原生 UI 测试启用 QUOTE_NATIVE_UI_TESTS=1，52 项全部通过（16.72 秒），合计 1996 项。
- 回归覆盖 1000×720 与 1280×850、先显示空值后接收价格、页面切换、下拉键盘/鼠标/滚轮操作。
- Ruff、mypy（87 个源文件）、git diff --check 通过。
- PyInstaller 独立目录构建，macOS codesign --verify --deep --strict 通过。
