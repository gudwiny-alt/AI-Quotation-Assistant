# 华为京东剩余失败：搜索与配送区域修复

基线 fe0c64a，分支 codex/mac-170-ui-refresh。仅修改京东适配器的配置标题匹配和配送区域定位；不修改报价策略、其他渠道、截图引擎或用户原始任务数据库。

## 原始任务与原因

用户最新一批真实运行共 18 条，17 条成功；剩余一条为华为京东：华为畅享 90 Pro Max、8GB / 256GB、曜金黑。原始错误为 `LAYOUT_CHANGED / JD exact product result is ambiguous`，并非华为官网失败。

实际店内搜索页有 9 张同型号商品卡。标题只显示存储容量和颜色，省略运行内存，因此原先要求 RAM+存储的完整标题匹配无法消除多 SKU 歧义。目标公开商品 URL 为 https://item.jd.com/100331677402.html。

搜索匹配复用详情页已经存在的固定 RAM 例外：仅华为畅享 90 Pro Max、任务 RAM 为 8GB 时，允许有单位的存储容量和完整颜色标题。其他 RAM、缺少单位或颜色、颜色前缀、多个目标 URL 仍拒绝。详情中的 SKU、选中配置、价格校验继续执行。

进入正确详情后发现第二个结构差异：页面存在“送至”地址控件，但没有 `.logistics-delivery-time` 预计送达文案。新增带 `data-id`、位于配送地址容器内的地址文本回退；要求唯一、可见且非空，旧的预计送达定位保持优先。

## 真实复测边界：尚未完整取价成功

使用 APP 的 `run_website_tasks` 和 `mac_visual_review_runtime_factory`，在独立临时数据库中复测原任务规格。由于本机专用 Chrome 启动慢，最后一次诊断脚本仅将 CDP 启动等待从 12 秒延长至 60 秒；未将此修改写入 APP，取价及截图规则未替换。

复测已越过上述两处结构错误，进入正确配置的价格校验。实际页面显示到手价 2140.18（购买按钮注明新人到手价）、补贴价 2149，没有可验证的当前 SKU 普通售价或优惠前价节点。宣传图片内的建议零售价不作为当前 SKU 报价依据。

最终结果仍为 `NO_VALID_SELLING_PRICE`，需要人工补充。没有生成合格报价或正式报价截图，不能将本条记为成功，也不能将测试结果表述为 18/18 成功。本次未重跑其余 17 条或改写整批 Excel。

保留补贴价格排除规则，并补充当前“到手价 + 补贴价”的局部结构回归用例。没有改取补贴价、搜索页价或历史价格来消除失败状态。

## 数据与验证

提交的搜索 fixture 仅含清洗后的公开商品标题及规范商品链接；账号、配送详细地址、跟踪参数和原始页面截图不提交仓库。原始任务库、报价工作簿和已成功结果均未修改。

搜索和地址回退分别先复现失败，再修复并运行合同测试；同时覆盖错误 RAM、缺失规格、重复 URL、隐藏/空白/重复地址等负例。

- 京东合同、网页运行服务、价格规则测试合计 658 项通过。
- 变更文件 Ruff 检查、2 个源文件 mypy、`git diff --check` 通过。
- PyInstaller 独立构建成功，`codesign --verify --deep --strict` 通过。
- 以上是自动化验证与构建检查，不代表华为京东实际取价通过。
