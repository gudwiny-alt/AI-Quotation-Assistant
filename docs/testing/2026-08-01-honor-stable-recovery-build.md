# 荣耀官网稳定恢复版构建记录

构建日期：2026-08-01

## 安装包

- 路径：`dist-honor-stable-recovery/福建移动铺货报价助手.app`
- 版本显示：`荣耀官网稳定恢复版（仅官网、全部荣耀行）2026.08.01.1`
- 主可执行文件 SHA-256：`afc1027c0701c6cc6087a93c86b2b8e6d37199de1046a6f43c5a6cc28a18e367`
- macOS 深度签名和严格校验：通过。

## 本包变更范围

- 荣耀官网首页搜索：点击后等待实际结果卡片；若未呈现，仅对同一搜索框补发一次 `Enter`；仍无结果则用 `HONOR_SEARCH_RESULTS_MISSING` 停止当前任务，不进行重复导航。
- 荣耀详情、SKU 选择、价格解析、截图语义校验和 Excel 写入链路保持原样，并受详情链路/荣耀 override 回归护栏保护。
- 最终报价表与最终执行报告均写成功后，自动清理本次运行的两份 `-处理中.xlsx` 检查点；异常路径仍保留检查点。
- 默认范围仍为“荣耀官网验收（仅官网）”；京东、天猫不会被本包启动。

## 验证证据

- 荣耀官网合同和冻结回归：31 项通过。
- 任务运行回归：34 项通过。
- 相关自动回归：184 项通过，静态类型检查通过。
- 完整 pytest 套件在允许本机临时端口的环境中退出成功；沙箱内唯一不可运行项是浏览器持久化测试创建 `127.0.0.1` 临时服务器的权限限制。
- PyInstaller 构建和 `codesign --verify --deep --strict --verbose=2` 均通过。

## 待真实验收

本记录不声明真实荣耀官网已通过。请用两条荣耀输入运行本包，重点确认：每条是否只在同一窗口进入对应产品页、`AK` 价格和 `AN` 截图是否逐行写入、最终输出目录是否只剩报价表与执行报告两份文件。截图滚动锚点将在取得这次真实详情页截图后再以证据决定，不在本包中猜测性修改。
