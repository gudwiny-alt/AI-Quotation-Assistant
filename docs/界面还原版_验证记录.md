# 界面还原版验证记录

日期：2026-09-13。基线：b4f7802；分支：codex/mac-170-ui-refresh。

## 本次调整

报价决策页按最终两张设计图恢复步骤条、商品渠道价格条、产品经理填报与智能报价提示双栏、优福包确认区、六列写回预览和底部保存操作。稽核页恢复四项统计、六类全批次统计、商品清单与单品检查、检查依据和实际证据区。导航、字号和留白统一调整。

参考尺寸普通报价和优福包待确认两种状态均可在首屏看到保存/确认按钮。窄窗口允许滚动；多状态大计数自动增高，避免信息丢失。已有业务功能入口保留。

## 验证结果

- 非原生全套：3073 passed，1项因沙箱禁止绑定127.0.0.1端口未执行成功。该项 `test_cookie_and_local_storage_survive_close_and_reopen` 在允许本地端口的环境单独补测：1 passed。合计3074项通过，无未解决的测试失败。
- 原生界面全套：59 passed，48.54秒；包含三种窗口尺寸、金额精度和可见性、键盘/滚动、草稿与真实临时工作簿写回、稽核联动及密集分类统计。
- Ruff、git diff --check 通过。
- 三张原生布局测试截图：3072×2008；回调异常为零。截图使用隔离测试数据，不是实际业务报价结果。
- PyInstaller 6.21.0 / Python 3.12.14，Apple Silicon arm64 打包成功。构建清单确认使用当前工作树的三个界面模块。
- codesign --verify --deep --strict、安装包 --help 均返回0。
- 新 App 实际启动主窗口和稽核工作台已显示。启动测试脚本的强制进程终止会触发 Tk 运行时退出诊断，因此不把强制终止作为正常关闭验证；正常窗口生命周期由原生测试覆盖。测试未运行真实网站取价。

## 原业务保护

与 b4f7802 比较，`app.py`、`services/`、`sites/`、`browser/`、`excel/` 无差异。原有取价、截图、计算、精确金额校验、工作簿安全写回及失败处理保持原实现。新 App 使用独立名称及 bundle identifier；旧 App 保留。

## 设计裁定

设计图决定界面布局，不决定实际业务是否通过。实际稽核项、计数、金额和证据按运行结果展示；通用手机素材采用用户已批准的本地预制方案。保留全部真实检查和操作入口。原生字体、控件绘制与生成图片不承诺逐像素一致。

详细视觉对比与迭代证据见项目根目录 `design-qa.md`。功能使用方式继续适用《报价决策与稽核增强版_使用说明》；本次主要优化呈现层。
