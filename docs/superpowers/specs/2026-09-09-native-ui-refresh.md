# Mac .170 界面改造

用户已选定既有 Tkinter App 快速改造，并批准三张蓝白工作台设计，同步授权开发。基线 94aaecb，独立分支 codex/mac-170-ui-refresh。视觉参考位于 /Users/yangguowei/.codex/generated_images/01a0856b-ac6a-7783-9ffa-c48c88c3f853/，三个 exec-*.png 为已批准设计。

## 目标与边界

在现有 Tkinter/ttk 技术中实现现代蓝白侧栏工作台：数据准备/协同总览、价格情报、报价决策、稽核审查、任务记录、系统设置；明细从实际事件、SQLite 及本次 FullPipelineResult 读取。缺失数据显示未获取，不生成模拟结果。内部系统数据仍通过本地 Excel 导入，用来源卡片、简短文件名和可展开完整路径展示。不要声称 BOSS 自动直采、毛利优化、自动审批已实现。

保留真实原有操作：文件选择、报价月份及范围、开始任务、首次登录、权限检查、继续等待任务、取消等待任务、打开输出。不要改取价、公式、截图、站点或任务恢复业务。保持后台线程与 Tk 主线程边界，不自动提到前台、不抢 Chrome 焦点。状态须区分价格已保存与截图完成、合法无价格结果与技术失败。不得编造顺序运行的三个后端阶段。

视觉：原生 Mac 标题栏，浅灰蓝背景、白色主体、蓝色选中导航和主按钮，清楚的标题/表格/状态/日志，不堆砌装饰。默认约 1280x850，适配小窗口。完整列表配滚动条。价格/稽核明细以当前真实数据能力为限，存在数据才能打开证据。约束冲突规划图仅用于概念材料，不新增生产毛利判断或审批。

## 验收

所有原有单元/集成测试继续通过；针对界面状态归并/价格与证据区别/新任务清空/历史只读/路径显示新增有意义测试。现场查看 Tk 界面验证布局、导航、空状态、文件选择及按钮绑定。不得为 UI 验证启动真实批量取价或改用户数据库。打包到独立 dist-mac-ui-refresh，不覆盖旧 App。清晰报告真实网站回归未执行的限制。
